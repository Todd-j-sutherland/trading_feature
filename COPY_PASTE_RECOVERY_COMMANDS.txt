# COPY AND PASTE THESE COMMANDS INTO YOUR SSH SESSION
# Run these commands one by one on your remote droplet

# 1. Navigate to project and activate environment
cd /root/test && source /root/trading_venv/bin/activate && export PYTHONPATH=/root/test

# 2. Install cron jobs
crontab simple_cron_jobs.txt

# 3. Start cron service
sudo service cron start && sudo systemctl enable cron

# 4. Test main application status
python -m app.main status

# 5. Test paper trading
cd /root/test/paper-trading-app && source ./paper_trading_venv/bin/activate && python run_paper_trading_ig.py test

# 6. Start paper trading service (if during market hours)
python run_paper_trading_ig.py service &

# 7. Verify cron jobs are working
crontab -l | grep -E "(morning|evening|paper)" | head -5

# 8. Check processes
ps aux | grep -E "(python|trading)"

# 9. Create logs directory and check
cd /root/test && mkdir -p logs && ls -la logs/

# 10. Quick health check
python -c "print('✅ System Recovery Complete!'); import datetime; print(f'Time: {datetime.datetime.now()}')"

echo "🚀 All recovery steps complete! System should be operational."
